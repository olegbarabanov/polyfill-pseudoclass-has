{
  "name": "polyfill-dom-selectors-api-pseudoclass-has",
  "version": "0.0.1-alpha.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -fr dist",
    "build": "npm run clean && tsc -p tsconfig.prod.json && npm run bundle:umd",
    "bundle:umd": "rollup dist/esm/index.js --file dist/umd/polyfill.umd.js --format umd --name polyfill --sourcemap",
    "test": "jest"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.8",
    "@types/node": "^18.7.14",
    "jest-environment-jsdom": "^29.5.0",
    "rollup": "^3.26.2",
    "ts-jest": "^28.0.8",
    "ts-loader": "9.3.1",
    "ts-node": "^10.9.1",
    "typescript": "4.7.4"
  },
  "jest": {
    "preset": "ts-jest",
    "globals": {
      "ts-jest": {
        "useESM": true,
        "isolatedModules": true
      }
    }
  }
}
